import { DateCreatedPipe } from './date-created.pipe';

describe('DateCreatedPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCreatedPipe();
    expect(pipe).toBeTruthy();
  });
});
